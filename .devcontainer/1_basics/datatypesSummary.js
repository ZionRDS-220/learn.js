// how data store in the memory are all data types that are devided into two parts 
// => primitive and non-premitive

// Primitive data types
// 7 types: are call by value
// string, number, boolean, null, undefined, symbol, bigInt



// Reference data types (non-premitive)
// array, objects, functions

// Arrays
// arrays are written in square brackets [] and define its value
// e.g, const heros = [supraman, batman, kraven]

// Objects
// objects are written in curly brackets {} with key value pairs
// e.g,
// {
// name; " Saad"
// Age ; "22"
// }

// functions
// function(){
//console.log("hello world")
//}

//how to know the datatype of any value
// console.log(typeof name of any variable);

// the return type of non-primitive datatype is (object) and of the function is (object function)



// ***************** Memory *********************

// Types of memory
// Stack and Heap

// Stack memory is used in (Primitive)
// Heap memory is used in ( non-Primitive)

// if stack memory is used then you get the copy of declared varible
//**** if heap memory is used then you get the reference of orignal value****

// let myNameis = "Saadi" 
// variable (myNameis) the primitive type then its value will go into the stack memory